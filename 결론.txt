* 실험의 성취 내용
type=module을 써줘야 모듈 스코프가 적용된다. 
안 써주면 모든 모듈이 각자의 변수를 공유해버린다. 
그렇게 되면.. 이거야 큰일이다.

* 참고
그리고 모듈(js 파일은) import나 script 태그로 불러와질 때 한 번 실행된다.
그래서 모듈에서 내보내지 않은 console.log(3)이 실행되어 콘솔에 3이 찍힌다.
